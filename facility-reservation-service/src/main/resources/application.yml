server:
  port: 9086

spring:
  application:
    name: facility-reservation-service
  config:
    import:
      - optional:classpath:application-common.yml
      - optional:classpath:application-jpa.yml
      - optional:classpath:application-kafka.yml
  datasource:
    driver-class-name: net.sf.log4jdbc.sql.jdbcapi.DriverSpy
    url: jdbc:log4jdbc:mysql://localhost:3306/apten_reservation?createDatabaseIfNotExist=true&characterEncoding=UTF-8&serverTimezone=Asia/Seoul
    username: root
    password: qaws7579
  kafka:
    consumer:
      group-id: facility-reservation-service-group

constants:
  api:
    prefix: /api
